#include <stdlib.h>

#include <time.h>

/**
 * main -function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
		int n;

		srand(time(0));
				n = rand() - RAND_MAX /

					if (n == 0)
				{
				printf("%d: " + n "is zero")
				}
				else if (n  > 0)
				{
				printf("%d:" + n + "is posetive")
				}
				else
				{
				printf("%d:" + n + "is negative")
				}

					return (0);

}
